# chatMind 依赖（语音陪伴 + 隐式HAMD评估）
# 另需先安装 torch/torchaudio（funasr依赖），版本按平台选择：
#   pip install torch==2.3.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cu118

funasr==1.1.12          # SenseVoice ASR
edge-tts==6.1.17        # 微软 Edge TTS
webrtcvad==2.0.10       # 语音活动检测
pygame==2.6.1           # 音频播放
PyAudio==0.2.14         # 麦克风采集
openai>=1.0             # 千问 DashScope（OpenAI兼容接口）
transformers==4.45.2    # SenseVoice 模型加载依赖
accelerate==0.33.0
